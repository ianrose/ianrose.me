{
  "name": "ianrose.me",
  "version": "0.1.0",
  "description": "Source for Ian Rose's personal website",
  "private": true,
  "main": "public/index.ejs",
  "author": "Ian Rose",
  "license": "Copyright © Ian Rose",
  "devDependencies": {
    "bower-installer": "^1.2.0",
    "browser-sync": "^2.9.8",
    "harp": "^0.20.1",
    "imagemin-cli": "^2.1.0",
    "jshint": "^2.9.1",
    "recursive-uglifyjs": "^1.0.0",
    "surge": "^0.17.7"
  },
  "scripts": {
    "postinstall": "bower-installer",
    "test": "jshint ./public/**/*.js",
    "dev": "harp server & browser-sync start --no-open --no-notify --proxy 'localhost:9000' --files './public/**/*.ejs, public/**/*.jade, ./public/**/*.ejs, public/**/*.md, ./public/**/*.scss, public/**/*.sass, ./public/**/*.less, ./public/**/*.css, ./public/**/_data.json'",
    "uglify": "recursive-uglifyjs ./www/",
    "copy:redirects": "cp ./public/_redirects www/",
    "compile": "harp compile && npm test",
    "postcompile": "npm run uglify && npm run imagemin && npm run copy:redirects",
    "imagemin": "imagemin ./public/images/ ./www/images/ --optimizationLevel 3 --progressive",
    "deploy": "surge --project ./www --domain ianrose.me"
  },
  "engines": {
    "node": "5.1.x",
    "npm": "3.3.x"
  }
}
