<% include _includes/typogr %>
<!doctype html>
<html lang="en" class="no-js">
  <% include _includes/head %>
  <body id="top" class="<%= current.path[0] %>" itemscope itemtype="http://schema.org/WebPage">
    <div class="nav--menu">
      <% include _includes/header %>
      <% include _includes/nav %>
    </div>
    <div class="layout--main">
      <div class="container">
        <main>
          <!--[if lte IE 9]>
          <p class="align-center">This is a portfolio site. I used this opportunity to experiment with <a href="http://caniuse.com/#feat=flexbox" title="Can I Use">CSS Flexbox</a>. Please use a <a href="http://browsehappy.com/" title="Browse Happy">modern browser</a>.</p>
          <![endif]-->

          <% if((current.path[0] == "work" || current.path[0] == "articles") && current.source !== 'index') { %>
            <%- partial(current.path[0] + "/_nest") %>
          <% } else { %>
            <%- typogr.smartypants(yield) %>
          <% } %>

        </main>
        <% include _includes/footer%>
      </div>
    </div>
    <% include _includes/scripts %>
    <% include _includes/analytics %>
  </body>
</html>
