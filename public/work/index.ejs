<h1 class="heading heading--page">Work</h1>
<section class="section row">
  <% for(var slug in public.work._data){ %>
  <% var entry = public.work._data[slug] %>
  <% var date = entry.date %>
  <% var modified = entry.modified %>
    <% include ../_includes/entry %>
  <% } %>
</section>
