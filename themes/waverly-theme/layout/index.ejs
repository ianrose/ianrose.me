<p class="heading heading--page"><%= config.description %></p>
<div class="row">
	<div class="section--id--l">
		<div class="section--id" aria-hidden="true">01</div>
	</div>
	<div class="section--posts">
		<div class="meta--container">
			<h2 class="meta--txt meta--primary meta--heading">Articles</h2>
		</div>
		<div class="row">
			<%site.categories.findOne({name: 'articles'}).posts.sort('date', -1).limit(14).each(function(post) {%>
			<%- partial('partials/entry-post', {post: post, index: true}) %>
			<% })%>
		</div>
	</div>
</div>
<div class="row">
	<div class="section--id--l">
		<div class="section--id" aria-hidden="true">02</div>
	</div>
	<div class="section--posts">
		<div class="meta--container">
			<h2 class="meta--txt meta--primary meta--heading">Work</h2>
		</div>
		<div class="row">
			<%site.categories.findOne({name: 'work'}).posts.sort('date', -1).limit(14).each(function(post) {%>
			<%- partial('partials/entry-work', {post: post, index: true}) %>
			<% })%>
		</div>
	</div>
	</div>
<div class="row">
	<div class="section--id--l">
		<div class="section--id" aria-hidden="true">03</div>
	</div>
	<div class="section--posts">
		<div class="meta--container">
			<h2 class="meta--txt meta--primary meta--heading">Notes</h2>
		</div>
		<div class="row">
			<%site.categories.findOne({name: 'notes'}).posts.sort('date', -1).limit(14).each(function(post) {%>
			<%- partial('partials/entry-post', {post: post, index: true}) %>
			<% })%>
		</div>
	</div>
</div>
