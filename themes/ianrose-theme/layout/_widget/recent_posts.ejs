<% if (site.posts.length) { %>
  <div class="widget-wrap">
  	<h4 class="heading--alt">Recents</h4>
    <div class="widget">
      <ul>
        <% site.posts.sort('date', -1).limit(5).each(function(post) { %>
          <li>
            <a href="<%- config.root %><%- post.path %>"><%= post.title || '(no title)' %></a>
          </li>
        <% }) %>
      </ul>
    </div>
  </div>
<% } %>
